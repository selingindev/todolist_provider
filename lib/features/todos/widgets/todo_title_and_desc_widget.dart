import 'package:flutter/material.dart';

class TodoTitleAndDescWidget extends StatelessWidget {
  const TodoTitleAndDescWidget({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Column(

    ),);
  }
}